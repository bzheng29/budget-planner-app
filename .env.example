# Gemini API Configuration
# Get your API key from: https://makersuite.google.com/app/apikey
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Override the model (defaults to gemini-2.5-pro)
VITE_GEMINI_MODEL=gemini-2.5-pro

# IMPORTANT SECURITY NOTES:
# 1. Never commit your actual API key to the repository!
# 2. Copy this file to .env and add your real API key there
# 3. The .env file is already in .gitignore to prevent accidental commits
# 4. For deployment, set the API key as an environment variable in your hosting platform